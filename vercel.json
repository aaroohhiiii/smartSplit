{
  
    "buildCommand": "npm install && cd backend && npx prisma generate && cd ../frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api",
      "destination": "/api/[...all]"
    },
    {
      "source": "/api/:path*",
      "destination": "/api/[...all]"
    }
  ]
}