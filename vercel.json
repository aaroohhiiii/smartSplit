{
  "buildCommand": "cd backend && npm run build",
  "outputDirectory": "backend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/[...route]"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
